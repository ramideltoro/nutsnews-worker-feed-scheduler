# Security Policy

Report security concerns privately to @ramideltoro.

Do not open public issues containing production secrets, credentials, tokens, host access material, or sensitive operational details.

Production runtime secrets belong only to the protected `production-backend` environment in `ramideltoro/nutsnews-backend`.
